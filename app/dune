(executable
  (name scirep_app)
  (public_name scirep)
  (modes byte)
  (libraries
    compiler-libs.toplevel
    core_kernel
    findlib.top
    omd
    scirep)
  (preprocess (pps ppx_blob))
  (preprocessor_deps (file template.html) (file style.css))
)
  
